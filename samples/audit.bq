script AuditScript

// Find PE magic bytes starting from a known load address
find bytes "4D 5A 90 00" from 0x00400000

// Find indirect call pattern anywhere in the binary (falls back to
// currentProgram.getMinAddress() when no 'from' clause is given).
find bytes "FF 25"
